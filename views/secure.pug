extends layout

block content
 br
 div(class="infoside")
  p(id='username') #{name}
  p(id='account') Account: #{account} 
   i(class="fas fa-address-card")
  button(type='button' class='indexbtn' onclick="location.href='/ads/myAd'") MY AD
  br
  br
  button(type='button' class='indexbtn' onclick="location.href='/ads/newAd'") CREATE AD
  br
  br
  button(type='button' class='indexbtn' onclick="location.href='/logout'")
   i(class='fas fa-sign-out-alt')
  br
  br
 div(class='midpage')
  h1 News
  p BidDon is web app for mutual donating of money. 
  p With every donate you get better rank and more privileges.
  p Backend running on node.js. Frontend made with pug(jade),css.Database is mysql.
