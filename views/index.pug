extends layout

block content
 div(class='title')
  p #{title}
 div(class='logreg')
  p Username: 
  input(type='text' id='username' autocomplete='off' name='username')
  p Password: 
  input(type='password' id='password' name='password')
  br
  button(type='button' class='indexbtn' onclick='Login()') Submit
  p(id='errorP')
  br
  p Don't have an account?
   button(type='button' class='indexbtn' onclick="location.href='/register'") REGISTER
  br
  br
